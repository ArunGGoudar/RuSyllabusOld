<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.arun.rusyllabus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.course.BeSyllabusActivity" />
        <activity android:name=".ui.branch.CseActivity" />
        <activity android:name=".ui.branch.CivilActivity" />
        <activity android:name=".ui.branch.EeeActivity" />
        <activity android:name=".ui.branch.EceActivity" />
        <activity android:name=".ui.branch.MechActivity" />
        <activity android:name=".ui.course.MTechActivity"></activity>
    </application>

</manifest>